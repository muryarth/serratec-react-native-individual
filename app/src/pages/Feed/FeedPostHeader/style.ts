import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  feedUserSection: {
    flexDirection: "row",
    alignItems: "center",
    gap: 12,
    paddingHorizontal: 12,
    paddingVertical: 8,
  },

  userNameText: {
    fontSize: 15,
  },

  userExtraText: {
    fontSize: 12,
  },
});
