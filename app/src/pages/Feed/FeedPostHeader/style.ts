import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  feedUserSection: {
    flexDirection: "row",
    alignItems: "center",
    gap: 10,
    paddingHorizontal: 12,
    paddingVertical: 13,
  },

  userNameText: {
    fontSize: 14,
  },

  userExtraText: {
    fontSize: 12,
  },
});
