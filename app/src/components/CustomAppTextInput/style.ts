import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  textInput: {
    backgroundColor: "#ededed",
    borderColor: "#c4c4c4",
    height: 37,
    borderWidth: 1,
    borderRadius: 4,
    width: "100%",
  },
});
